FROM rayruan/yocto:basic


ARG user
ARG uid
ARG group
ARG gid

RUN groupadd -r "$group" -g "$gid" \
    && useradd -r -m -u "$uid" -g "$gid" "$user" \
    && usermod -aG $group root

COPY ./.vim /home/ray/
COPY ./.vimrc /home/ray/
COPY ./.vimrc.local /home/ray/


#USER $user


